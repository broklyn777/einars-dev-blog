---
title: Next js code
date: "2020-05-09"
description: The reduce function of an array is one of the most versatile functions in JavaScript.
thumbnailUrl: "/image/reduce.jpg"
tags: ["arrays", "reduce"]
---

<div>
  This is a blog post about <strong>SolidJS</strong>. <br />
  We'll learn about SolidJS and how to build with it.
</div>

<br />

### This is a Markdown sub-heading

We just used a `###` subheading in our blog post.

<SyntaxHighlighter language="javascript">
  {`
const PostPage = ({ frontMatter: { title, date }, mdxSource }) => {
  return (
    <SectionContainer>

      <div className="mt-4 ">
         <div className="prose">
        <h1>{title}</h1>
           <MDXRemote {...mdxSource} components={components} />
         </div>
      </div>
    </SectionContainer>

)
}

`}

</SyntaxHighlighter>
<SyntaxHighlighter language="css">
  {`
    .container {
  position: relative;
  padding-bottom: 56.25%;
  height: 0;
  overflow: hidden;
  max-width: 100%;
}

.frame {
position: absolute;
top: 0;
left: 0;
width: 100%;
height: 100%;
border: 0;
}
`}

</SyntaxHighlighter>

```
  // this is a code block
  const doSomething = () => {
    return console.log('hey')
  }
```

<div>
  Here, I'm using a component inside my MarkDown file:{" "}
  {<Button text={"Click me"} />}
</div>

```
import styles from "@/styles/YouTube.module.css";

export default function YouTube({ id }: { id: string }) {
  return (
    <div className={styles.container}>
      <iframe
        src={`https://www.youtube.com/embed/${id}`}
        allow="autoplay; encrypted-media"
        title="Embedded YouTube video"
        className={styles.frame}
      />
    </div>
  );
}
```
